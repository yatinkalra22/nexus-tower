import type { Metadata } from "next";

export const metadata: Metadata = { title: "New Shipment | NexusTower" };

export default function NewShipmentLayout({ children }: { children: React.ReactNode }) {
  return children;
}
