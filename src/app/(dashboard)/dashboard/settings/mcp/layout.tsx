import type { Metadata } from "next";

export const metadata: Metadata = { title: "MCP Settings | NexusTower" };

export default function McpSettingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
